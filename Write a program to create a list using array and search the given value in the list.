#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, searchValue, found = 0;

   
    scanf("%d", &n);

    int list[n];

  
    for (int i = 0; i < n; i++) {
        
        scanf("%d", &list[i]);
    }

   
    scanf("%d", &searchValue);

   
    for (int i = 0; i < n; i++) {
        if (list[i] == searchValue) {
            found = 1;
            break;
        }
    }

    
    if (found) {
        printf("Found\n");
    }else{
        printf("Not Found\n");
    }
    

    return 0;
}

  
